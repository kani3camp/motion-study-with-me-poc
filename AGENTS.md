# AGENTS.md

## Cursor Cloud specific instructions

This repository (`motion-study-with-me-poc`) is a freshly initialized proof-of-concept project. As of the initial setup, the repo contains only a `README.md` placeholder — no source code, dependencies, build system, or services exist yet.

- **No dependencies to install** — no `package.json`, `requirements.txt`, or similar manifest exists.
- **No services to run** — no application entry points are defined.
- **No tests or lint** — no test framework or linter is configured.

Once the project gains source code and a dependency manifest, update the VM environment script accordingly (e.g., `npm install`, `pip install -r requirements.txt`).
